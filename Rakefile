# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotion/lib")
require 'motion/project/template/ios'
require 'bundler'
Bundler.require

Motion::Project::App.setup do |app|
  
  app.name = 'Koalio'
  app.identifier = 'koalio.joybox.io'
  app.interface_orientations = [:landscape_left]
end
